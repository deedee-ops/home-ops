---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/kopiur.home-operations.com/snapshotschedule_v1alpha1.json
apiVersion: kopiur.home-operations.com/v1alpha1
kind: SnapshotSchedule
metadata:
  name: ${APP}
spec:
  policyRef:
    name: ${APP}
  schedule:
    cron: ${KOPIUR_SCHEDULE:="H * * * *"}
