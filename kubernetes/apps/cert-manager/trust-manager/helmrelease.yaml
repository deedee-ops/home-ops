---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/other/trust-manager-hr.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: trust-manager
spec:
  chartRef:
    kind: OCIRepository
    name: trust-manager
  interval: 1h
  values:
    app:
      podLabels:
        egress.cilium.io/kube-apiserver: "true"
