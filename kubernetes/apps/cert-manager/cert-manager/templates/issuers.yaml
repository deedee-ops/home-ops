---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/cert-manager.io/clusterissuer_v1.json
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-production
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: "letsencrypt@<path:kubernetes/data/internal/smtp-relay#INGRESS_DOMAIN>"
    privateKeySecretRef:
      name: letsencrypt-production
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: cloudflare-secret
              key: CLOUDFLARE_API_TOKEN
        selector:
          dnsZones:
            - "<path:kubernetes/data/internal/base#ROOT_DOMAIN>"

---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/cert-manager.io/clusterissuer_v1.json
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: "letsencrypt@<path:kubernetes/data/internal/smtp-relay#INGRESS_DOMAIN>"
    privateKeySecretRef:
      name: letsencrypt-staging
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: cloudflare-secret
              key: CLOUDFLARE_API_TOKEN
        selector:
          dnsZones:
            - "<path:kubernetes/data/internal/base#ROOT_DOMAIN>"
