---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/pocketid.internal/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: renovate-operator
spec:
  pkceEnabled: true
  clientID: 497f956c-92f8-463f-951f-ce1b0acc4b71
  launchUrl: https://renovate.${ROOT_DOMAIN}
  callbackUrls:
    - "https://renovate.${ROOT_DOMAIN}/auth/callback"
  allowedUserGroups:
    - name: admins
      namespace: security
  secret:
    additionalLabels:
      reconcile.fluxcd.io/watch: Enabled
