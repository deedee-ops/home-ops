---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/renovate-operator.mogenius.com/renovatejob_v1alpha1.json
apiVersion: renovate-operator.mogenius.com/v1alpha1
kind: RenovateJob
metadata:
  name: forgejo
spec:
  discoveryFilters:
    - deedee/claude
    - deedee/mark13
    - deedee/schemas
  extraEnv:
    - name: LOG_LEVEL
      value: debug
    - name: GIT_SSL_CAINFO
      value: &certs /etc/ssl/certs/ca-certificates.crt
    - name: NODE_EXTRA_CA_CERTS
      value: *certs
    - name: RENOVATE_DETECT_HOST_RULES_FROM_ENV
      value: "true"
  extraVolumes:
    - name: homelab-root-ca
      configMap:
        name: homelab-root-ca
        defaultMode: 420
  extraVolumeMounts:
    - mountPath: /etc/ssl/certs/ca-certificates.crt
      name: homelab-root-ca
      subPath: ca.crt
      readOnly: true
  image: ghcr.io/renovatebot/renovate:44.32.4
  metadata:
    labels:
      app.kubernetes.io/name: renovate-job
      egress.cilium.io/internet: "true"
  parallelism: 5
  provider:
    name: forgejo
    endpoint: https://git.${ROOT_DOMAIN}/api/v1
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
  schedule: 0 * * * *
  secretRef: renovate-secret
  webhook:
    enabled: false
