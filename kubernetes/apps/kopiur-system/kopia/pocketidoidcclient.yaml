---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/pocketid.internal/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: kopia
spec:
  pkceEnabled: true
  launchUrl: https://kopia-nas.ajgon.casa
  callbackUrls:
    - https://kopia-b2.${ROOT_DOMAIN}/oauth2/callback
    - https://kopia-nas.${ROOT_DOMAIN}/oauth2/callback
  allowedUserGroups:
    - name: admins
      namespace: security
  secret:
    keys:
      clientID: client-id
      clientSecret: client-secret
