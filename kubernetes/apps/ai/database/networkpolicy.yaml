---
# yaml-language-server: $schema=https://schemas.ajgon.casa/cilium.io/ciliumnetworkpolicy_v2.json
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: ai-egress-databases
spec:
  endpointSelector:
    matchLabels:
      cnpg.io/cluster: ai-db
  egress:
    - toEndpoints:
        - matchLabels:
            cnpg.io/cluster: ai-db
