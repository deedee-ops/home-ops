# yamllint disable rule:line-length
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master-standalone/secret-v1.json
apiVersion: v1
kind: Secret
metadata:
  name: restic-syncthing-syncthing-config
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "<path:kubernetes/data/internal/base#S3_ACCESS_KEY_ID>"
  AWS_SECRET_ACCESS_KEY: "<path:kubernetes/data/internal/base#S3_SECRET_ACCESS_KEY>"
  RESTIC_PASSWORD: "<path:kubernetes/data/internal/volsync#RESTIC_PASSWORD>"
  RESTIC_REPOSITORY: "s3:<path:kubernetes/data/internal/base#S3_URL>/<path:kubernetes/data/internal/base#S3_BUCKET>/volsync/syncthing-config"
---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/volsync.backube/replicationsource_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: backup-syncthing-syncthing-config
spec:
  sourcePVC: syncthing-config
  trigger:
    schedule: "30 1 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 7
    repository: restic-syncthing-syncthing-config
    volumeSnapshotClassName: ceph-block
    cacheCapacity: 128Mi
    cacheStorageClassName: ceph-persistent-block
    cacheAccessModes:
      - ReadWriteOnce
    storageClassName: ceph-persistent-block
    accessModes:
      - ReadWriteOnce
    retain:
      daily: 7
      weekly: 4
      monthly: 6
      yearly: 3
      within: 3d
    moverSecurityContext:
      runAsNonRoot: true
      fsGroup: 65000
      runAsUser: 65000
      runAsGroup: 65000
      seccompProfile:
        type: RuntimeDefault
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master-standalone/persistentvolumeclaim.json
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncthing-config
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  accessModes:
    - ReadWriteOnce
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: init-syncthing-syncthing-config
  resources:
    requests:
      storage: 128Mi
  storageClassName: ceph-persistent-block
---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/volsync.backube/replicationdestination_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: init-syncthing-syncthing-config
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  trigger:
    manual: restore-init
  restic:
    repository: restic-syncthing-syncthing-config
    copyMethod: Snapshot
    volumeSnapshotClassName: ceph-block
    cacheStorageClassName: ceph-persistent-block
    cacheAccessModes:
      - ReadWriteOnce
    cacheCapacity: 128Mi
    storageClassName: ceph-persistent-block
    accessModes:
      - ReadWriteOnce
    capacity: 128Mi
    moverSecurityContext:
      runAsNonRoot: true
      fsGroup: 65000
      runAsUser: 65000
      runAsGroup: 65000
      seccompProfile:
        type: RuntimeDefault
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master-standalone/secret-v1.json
apiVersion: v1
kind: Secret
metadata:
  name: restic-syncthing-syncthing-data
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "<path:kubernetes/data/internal/base#S3_ACCESS_KEY_ID>"
  AWS_SECRET_ACCESS_KEY: "<path:kubernetes/data/internal/base#S3_SECRET_ACCESS_KEY>"
  RESTIC_PASSWORD: "<path:kubernetes/data/internal/volsync#RESTIC_PASSWORD>"
  RESTIC_REPOSITORY: "s3:<path:kubernetes/data/internal/base#S3_URL>/<path:kubernetes/data/internal/base#S3_BUCKET>/volsync/syncthing-data"
---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/volsync.backube/replicationsource_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: backup-syncthing-syncthing-data
spec:
  sourcePVC: syncthing-data
  trigger:
    schedule: "30 1 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 7
    repository: restic-syncthing-syncthing-data
    volumeSnapshotClassName: ceph-block
    cacheCapacity: 20Gi
    cacheStorageClassName: ceph-persistent-block
    cacheAccessModes:
      - ReadWriteOnce
    storageClassName: ceph-persistent-block
    accessModes:
      - ReadWriteOnce
    retain:
      daily: 7
      weekly: 4
      monthly: 6
      yearly: 3
      within: 3d
    moverSecurityContext:
      runAsNonRoot: true
      fsGroup: 65000
      runAsUser: 65000
      runAsGroup: 65000
      seccompProfile:
        type: RuntimeDefault
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/master-standalone/persistentvolumeclaim.json
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syncthing-data
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  accessModes:
    - ReadWriteOnce
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: init-syncthing-syncthing-data
  resources:
    requests:
      storage: 20Gi
  storageClassName: ceph-persistent-block
---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/volsync.backube/replicationdestination_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: init-syncthing-syncthing-data
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  trigger:
    manual: restore-init
  restic:
    repository: restic-syncthing-syncthing-data
    copyMethod: Snapshot
    volumeSnapshotClassName: ceph-block
    cacheStorageClassName: ceph-persistent-block
    cacheAccessModes:
      - ReadWriteOnce
    cacheCapacity: 20Gi
    storageClassName: ceph-persistent-block
    accessModes:
      - ReadWriteOnce
    capacity: 20Gi
    moverSecurityContext:
      runAsNonRoot: true
      fsGroup: 65000
      runAsUser: 65000
      runAsGroup: 65000
      seccompProfile:
        type: RuntimeDefault
# yamllint enable rule:line-length
