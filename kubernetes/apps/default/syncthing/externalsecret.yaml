---
# yaml-language-server: $schema=https://schemas.ajgon.casa/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: syncthing
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: syncthing-secret
    template:
      data:
        client-id: "{{ .SYNCTHING_CLIENT_ID }}"
        client-secret: "{{ .SYNCTHING_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: kubernetes
