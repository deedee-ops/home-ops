---
app-template:
  defaultPodOptions:
    labels:
      # fetching videos metadata
      egress/enabled: "true"
    securityContext:
      fsGroup: 65000
      runAsNonRoot: true
      seccompProfile:
        type: RuntimeDefault

  controllers:
    main:
      pod:
        nodeSelector:
          intel.feature.node.kubernetes.io/gpu: "true"

      containers:
        main:
          image:
            repository: ghcr.io/deedee-ops/jellyfin
            tag: 10.8.13
            pullPolicy: IfNotPresent

          env:
            DOTNET_SYSTEM_IO_DISABLEFILELOCKING: "true"
            JELLYFIN_PublishedServerUrl: "<path:kubernetes/data/internal/jellyfin#IP_INTERNAL>"
            TZ: Europe/Warsaw

          securityContext:
            runAsNonRoot: true
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

          probes:
            startup:
              enabled: false
            readiness: &probes
              custom: true
              spec:
                httpGet:
                  path: /health
                  port: 8096
                initialDelaySeconds: 0
                periodSeconds: 10
                timeoutSeconds: 1
                failureThreshold: 3
            liveness: *probes

          resources:
            requests:
              cpu: 100m
              memory: 1Gi
              gpu.intel.com/i915: 1
            limits:
              memory: 4Gi
              gpu.intel.com/i915: 1

  service:
    main:
      type: LoadBalancer
      annotations:
        io.cilium/lb-ipam-ips: "<path:kubernetes/data/internal/jellyfin#IP_INTERNAL>"
      ports:
        http:
          port: 8096

  serviceMonitor:
    main:
      enabled: true

  ingress:
    main:
      enabled: true
      className: internal
      annotations:
        nginx.ingress.kubernetes.io/enable-global-auth: "false"
        nginx.ingress.kubernetes.io/configuration-snippet: >-
          more_set_headers "Content-Security-Policy:
          default-src 'self' 'unsafe-inline' data: blob: wss:;
          img-src 'self' data: https://repo.jellyfin.org https://raw.githubusercontent.com;
          object-src 'none';
          style-src 'self' 'unsafe-inline' data: blob: *.<path:kubernetes/data/internal/base#ROOT_DOMAIN>;
          report-uri https://csp.<path:kubernetes/data/internal/base#ROOT_DOMAIN>/report/csp/jellyfin;";
        gethomepage.dev/enabled: "true"
        gethomepage.dev/group: Media
        gethomepage.dev/name: Jellyfin
        gethomepage.dev/icon: jellyfin.png
        gethomepage.dev/description: Media Streaming
      hosts:
        - host: "jellyfin.<path:kubernetes/data/internal/base#ROOT_DOMAIN>"
          paths:
            - path: /
              pathType: Prefix
              service:
                name: main
                port: 8096
      tls:
        - hosts:
            - "jellyfin.<path:kubernetes/data/internal/base#ROOT_DOMAIN>"

  persistence:
    config:
      enabled: true
      type: persistentVolumeClaim
      existingClaim: jellyfin-config
      advancedMounts:
        main:
          main:
            - path: "/config"
    transcode:
      enabled: true
      type: emptyDir
      medium: Memory
    video:
      enabled: true
      type: persistentVolumeClaim
      existingClaim: jellyfin-video
      advancedMounts:
        main:
          main:
            - path: "/data"
              readOnly: true
