---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/other/app-template-hr.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kiwix
spec:
  chartRef:
    kind: OCIRepository
    name: kiwix
  interval: 1h
  values:
    controllers:
      main:
        annotations:
          glance/name: Kiwix
          glance/icon: di:kiwix-light
          glance/url: https://kiwix.${ROOT_DOMAIN}
        containers:
          app:
            image:
              repository: ghcr.io/kiwix/kiwix-serve
              tag: 3.8.2@sha256:3f2f78a5e1b69e15a89ab7f5264be5cf675bf565282ef63f5fa0e6bc88d3866d
            args:
              - -i
              - ipv4
              - --library
              - /data/library.xml
            env:
              TZ: Europe/Warsaw
            probes:
              liveness:
                enabled: true
                spec:
                  periodSeconds: 30
                  timeoutSeconds: 5
                  failureThreshold: 5
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 8080
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 5
              startup:
                enabled: true
                spec:
                  failureThreshold: 30
                  periodSeconds: 10
            securityContext: &sc
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
            resources:
              requests:
                memory: "256Mi"
              limits:
                memory: "4096Mi"
        initContainers:
          01-library:
            image:
              repository: ghcr.io/kiwix/kiwix-tools
              tag: 3.8.2@sha256:cd0d74d5001fd82ba49ec5984484230aed3ff95ddf6857ba0f155676ae29aa32
            command:
              - /bin/sh
              - -c
              - kiwix-manage /data/library.xml add /data/*.zim
            securityContext: *sc
      cron:
        type: cronjob
        cronjob:
          schedule: "0 10 * * 3" # every Wednesday
          successfulJobsHistory: 0
          failedJobsHistory: 5
          concurrencyPolicy: Forbid

        containers:
          sync:
            image:
              repository: registry.ajgon.casa/tools/kiwix-zim-updater
              tag: latest@sha256:cd87e0c47e7534c32f477221bc2521aba933417c7a3e3671a14756685fe915d4
              pullPolicy: IfNotPresent
            securityContext: *sc
            resources:
              requests:
                cpu: 10m
                memory: 128Mi
              limits:
                memory: 256Mi
    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      dnsConfig:
        options:
          - name: ndots
            value: "1"
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
    service:
      app:
        controller: main
        ports:
          http:
            targetPort: 8080
            port: 80
    route:
      app:
        hostnames:
          - "kiwix.${ROOT_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
        rules:
          - backendRefs:
              - name: kiwix
                port: 80
            filters:
              - type: ResponseHeaderModifier
                responseHeaderModifier:
                  set:
                    - name: Content-Security-Policy
                      value: >-
                        default-src 'self' data:;
                        object-src 'none';
                        script-src 'self' 'unsafe-inline' 'unsafe-eval';
                        style-src 'self' 'unsafe-inline';
    persistence:
      tmp:
        type: emptyDir
        advancedMounts:
          cron:
            sync:
              - subPath: tmp
                path: /tmp
      zim:
        type: custom
        volumeSpec:
          csi:
            driver: nfs.csi.k8s.io
            volumeAttributes:
              server: nas.internal
              share: "/mnt/tank/media"
              mountOptions: "noac,nfsvers=4.1,nconnect=16,hard,noatime"
        globalMounts:
          - path: /data
            subPath: kiwix
