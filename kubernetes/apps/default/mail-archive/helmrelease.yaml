---
# yaml-language-server: $schema=https://schemas.ajgon.casa/other/app-template-hr.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mail-archive
spec:
  chartRef:
    kind: OCIRepository
    name: mail-archive
  interval: 1h
  values:
    controllers:
      main:
        annotations:
          glance/name: Mail Archive
          glance/icon: di:roundcube
          glance/url: https://mail.${ROOT_DOMAIN}
        pod:
          labels:
            egress.cilium.io/auth: "true"
        containers:
          app:
            image:
              repository: docker.io/roundcube/roundcubemail
              tag: "1.7.1-apache-nonroot@sha256:ce31fa7f7e441407c13161f9e514531be9a442448bda46fdbb509012f5d3c61e"
            env:
              ROUNDCUBEMAIL_DB_TYPE: "sqlite"
              ROUNDCUBEMAIL_DEFAULT_HOST: "mail-archive-dovecot"
              ROUNDCUBEMAIL_DEFAULT_PORT: "143"
              ROUNDCUBEMAIL_PLUGINS: "hide_blockquote,identicon,zipdownload"
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /
                    port: 8000
                  initialDelaySeconds: 0
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false # roundcube uses it's own codebase directory for writes
              capabilities:
                drop:
                  - ALL
              runAsUser: 33
              runAsGroup: 33
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 1Gi
          dovecot:
            image:
              repository: ghcr.io/slusarz/dovecot-fts-flatcurve
              tag: "v1.0.5@sha256:3a11df7028e476e2c0b971907cb48a525d37276961bcc44bfb7360f49950236c"
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
            securityContext: &sc
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
              runAsUser: 1000
              runAsGroup: 1000
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 256Mi
          tika:
            image:
              repository: docker.io/apache/tika
              tag: 3.3.1.0@sha256:90b7fa1dc018434075fce9e1d9b88b1e3d0ea6979d0cf86e116c79a8073ae973
            securityContext: *sc
            resources:
              requests:
                cpu: "10m"
                memory: "256Mi"
              limits:
                cpu: "1000m"
                memory: "1Gi"
            probes:
              liveness: &tikaProbes
                enabled: true
                spec:
                  tcpSocket:
                    port: 9998
              readiness: *tikaProbes

      imapsync:
        type: cronjob
        cronjob:
          schedule: "30 * * * *" # every hour
          successfulJobsHistory: 0
          failedJobsHistory: 5
          concurrencyPolicy: Forbid

        containers:
          sync:
            image:
              repository: docker.io/gilleslamiral/imapsync
              tag: "2.319@sha256:161336e1a6db587bc42ea1126cfc9b6afa67ea92b408ea4c4454f7f771561aa4"
              pullPolicy: IfNotPresent
            command:
              - sh
              - /imapsync.sh
            securityContext:
              runAsNonRoot: true
              runAsUser: 1000
              runAsGroup: 1000
              readOnlyRootFilesystem: true
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                  - ALL

            resources:
              requests:
                cpu: 100m
                memory: 64Mi
              limits:
                memory: 1Gi

        pod:
          dnsConfig:
            options:
              - name: ndots
                value: "1"
          securityContext:
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile:
              type: RuntimeDefault

    defaultPodOptions:
      automountServiceAccountToken: false
      enableServiceLinks: false
      securityContext:
        runAsNonRoot: true
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
    service:
      app:
        controller: main
        ports:
          http:
            port: 80
            targetPort: 8000
      dovecot:
        controller: main
        ports:
          imap:
            port: 143
    route:
      app:
        rules:
          - backendRefs:
              - name: mail-archive-app
                namespace: default
                port: 80
            filters:
              - type: ResponseHeaderModifier
                responseHeaderModifier:
                  set:
                    - name: Content-Security-Policy
                      value: >-
                        default-src 'self';
                        object-src 'none';
                        script-src 'self' 'unsafe-inline' 'unsafe-eval';
                        style-src 'self' 'unsafe-inline';
        hostnames:
          - "mail.${ROOT_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
    persistence:
      config:
        existingClaim: "mail-archive"
        advancedMounts:
          main:
            app:
              - subPath: "roundube"
                path: /var/roundcube/db
            dovecot:
              - subPath: "mail"
                path: /srv/mail
      dovecot-config:
        type: configMap
        name: mail-archive-configmap
        advancedMounts:
          main:
            dovecot:
              - subPath: "dovecot.conf"
                path: /etc/dovecot/dovecot.conf
                readOnly: true
          imapsync:
            sync:
              - subPath: "imapsync.sh"
                path: /imapsync.sh
      dovecot-password:
        type: secret
        name: mail-archive-secret
        advancedMounts:
          main:
            dovecot:
              - subPath: "DOVECOT_PASSWD"
                path: /etc/dovecot/passwd
                readOnly: true
          imapsync:
            sync:
              - path: /secrets
      extra-configs:
        type: configMap
        name: mail-archive-configs-configmap
        advancedMounts:
          main:
            app:
              - subPath: roundcube.oauth.inc.php
                path: /var/roundcube/config/roundcube.oauth.inc.php
                readOnly: true
            dovecot:
              - subPath: dovecot.oauth.conf
                path: /etc/dovecot/dovecot.oauth.conf
                readOnly: true
      homelab-ca:
        type: secret
        name: homelab-root-ca.crt
        advancedMounts:
          main:
            app:
              - path: /etc/ssl/certs/ca-certificates.crt
                subPath: ec.crt
            dovecot:
              - path: /etc/ssl/certs/ca-certificates.crt
                subPath: ec.crt
      imapsync:
        existingClaim: "mail-archive-sync"
        advancedMounts:
          imapsync:
            sync:
              - path: /data
      tmp:
        type: emptyDir
        globalMounts:
          - subPath: tmp
            path: /tmp
          - subPath: vartmp
            path: /var/tmp
        advancedMounts:
          main:
            dovecot:
              - subPath: dovecot-run
                path: /run/dovecot
              - subPath: dovecot-lib
                path: /usr/local/var/lib
