---
# yaml-language-server: $schema=https://schemas.ajgon.casa/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: crypt
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: crypt-secret
    template:
      data:
        client-id: "{{ .CRYPT_CLIENT_ID }}"
        client-secret: "{{ .CRYPT_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: kubernetes
