---
# yaml-language-server: $schema=https://schemas.ajgon.casa/postgresql.cnpg.io/scheduledbackup_v1.json
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: oxicloud-db
spec:
  cluster:
    name: oxicloud-db
  schedule: '0 0 * * * *'
  backupOwnerReference: self
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
