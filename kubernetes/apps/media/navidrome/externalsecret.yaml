---
# yaml-language-server: $schema=https://schemas.ajgon.casa/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: navidrome
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: navidrome-secret
    template:
      data:
        client-id: "{{ .NAVIDROME_CLIENT_ID }}"
        client-secret: "{{ .NAVIDROME_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: kubernetes
