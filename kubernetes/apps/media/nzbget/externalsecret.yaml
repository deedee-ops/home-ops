---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nzbget
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: nzbget-secret
    template:
      data:
        client-id: "{{ .NZBGET_CLIENT_ID }}"
        client-secret: "{{ .NZBGET_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: kubernetes
