---
# yaml-language-server: $schema=https://schemas.ajgon.casa/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pinchflat
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: pinchflat-secret
    template:
      data:
        client-id: "{{ .PINCHFLAT_CLIENT_ID }}"
        client-secret: "{{ .PINCHFLAT_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: kubernetes
