---
# yaml-language-server: $schema=https://schemas.ajgon.casa/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pyload
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: pyload-secret
    template:
      data:
        client-id: "{{ .PYLOAD_CLIENT_ID }}"
        client-secret: "{{ .PYLOAD_CLIENT_SECRET }}"
  dataFrom:
    - extract:
        key: kubernetes
