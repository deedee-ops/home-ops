---
# yaml-language-server: $schema=https://schemas.ajgon.casa/other/external-dns-hr.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app external-dns-unifi
spec:
  chartRef:
    kind: OCIRepository
    name: unifi-dns
  interval: 1h
  values:
    fullnameOverride: *app
    podLabels:
      egress.cilium.io/kube-apiserver: "true"
    dnsConfig:
      options:
        - name: ndots
          value: "1"
    provider:
      name: webhook
      webhook:
        image:
          repository: ghcr.io/home-operations/external-dns-unifi-webhook
          tag: 0.10.8@sha256:573ac443ea24b1769a3339a5252d06ed79ea4c1041a6172e7df218ecad3a583a
        env:
          - name: UNIFI_HOST
            value: https://unifi.internal
          - name: UNIFI_API_KEY
            valueFrom:
              secretKeyRef:
                name: &secret external-dns-unifi-secret
                key: UNIFI_API_KEY
        livenessProbe:
          httpGet:
            path: /healthz
            port: http-webhook
          initialDelaySeconds: 10
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /readyz
            port: http-webhook
          initialDelaySeconds: 10
          timeoutSeconds: 5
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          capabilities:
            drop:
              - ALL
    triggerLoopOnEvent: true
    policy: sync
    sources:
      - gateway-httproute
      - service
    serviceMonitor:
      enabled: true
    resources:
      requests:
        cpu: 10m
        memory: 100Mi
      limits:
        memory: 300Mi
    txtOwnerId: "${CLUSTER}"
    txtPrefix: "k8s.${CLUSTER}.%{record_type}-"
