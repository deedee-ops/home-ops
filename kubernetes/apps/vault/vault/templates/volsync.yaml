---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/volsync.backube/replicationsource_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: vault-data-vault-0
spec:
  sourcePVC: data-vault-0
  trigger:
    schedule: "30 1 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 7
    repository: restic-vault-data-vault-0
    retain:
      daily: 7
      weekly: 4
      monthly: 6
      yearly: 3
      within: 3d
    volumeSnapshotClassName: ceph-block
    # class name of cache temporary volume, not source PVC
    storageClassName: ceph-ephemeral-block
    moverSecurityContext:
      fsGroup: 1000
      runAsUser: 1000
      runAsGroup: 100
---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/volsync.backube/replicationsource_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: vault-data-vault-1
spec:
  sourcePVC: data-vault-1
  trigger:
    schedule: "30 1 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 7
    repository: restic-vault-data-vault-1
    retain:
      daily: 7
      weekly: 4
      monthly: 6
      yearly: 3
      within: 3d
    volumeSnapshotClassName: ceph-block
    # class name of cache temporary volume, not source PVC
    storageClassName: ceph-ephemeral-block
    moverSecurityContext:
      fsGroup: 1000
      runAsUser: 1000
      runAsGroup: 100
---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/volsync.backube/replicationsource_v1alpha1.json
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: vault-data-vault-2
spec:
  sourcePVC: data-vault-2
  trigger:
    schedule: "30 1 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 7
    repository: restic-vault-data-vault-2
    retain:
      daily: 7
      weekly: 4
      monthly: 6
      yearly: 3
      within: 3d
    volumeSnapshotClassName: ceph-block
    # class name of cache temporary volume, not source PVC
    storageClassName: ceph-ephemeral-block
    moverSecurityContext:
      fsGroup: 1000
      runAsUser: 1000
      runAsGroup: 100
