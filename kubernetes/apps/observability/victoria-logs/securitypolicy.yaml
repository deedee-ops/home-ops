---
# yaml-language-server: $schema=https://schemas.ajgon.casa/gateway.envoyproxy.io/securitypolicy_v1alpha1.json
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: victoria-logs
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: victoria-logs-server
  oidc:
    provider:
      issuer: "https://id.ajgon.casa"
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: pocket-id
          namespace: security
          port: 443
    clientIDRef:
      group: ""
      kind: Secret
      name: victoria-logs-secret
    clientSecret:
      group: ""
      kind: Secret
      name: victoria-logs-secret
    redirectURL: "https://logs.${ROOT_DOMAIN}/oauth2/callback"
    logoutPath: "/logout"
