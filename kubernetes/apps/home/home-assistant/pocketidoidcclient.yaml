---
# yaml-language-server: $schema=https://schemas.ajgon.casa/pocketid.internal/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: home-assistant
spec:
  launchUrl: https://home.${ROOT_DOMAIN}
  # TODO:
  # callbackUrls:
  #   - "https://home.${ROOT_DOMAIN}/oauth2/callback"
  secret:
    keys:
      clientID: HASS_OIDC_CLIENT_ID
      clientSecret: HASS_OIDC_CLIENT_SECRET
