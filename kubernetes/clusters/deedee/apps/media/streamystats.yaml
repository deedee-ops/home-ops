---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: streamystats
spec:
  components:
    - ../../../components/pgbackup
  interval: 1h
  path: ./kubernetes/apps/media/streamystats
  postBuild:
    substitute:
      APP: streamystats
      DBCLUSTER: media
      ROOT_DOMAIN: "${ROOT_DOMAIN}"
  patches: []
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
