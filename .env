export BWS_SERVER_URL="https://vault.bitwarden.eu"
export KUBECONFIG="${ROOT_DIR}/kubeconfig"
export MINIJINJA_CONFIG_FILE="${ROOT_DIR}/.minijinja.toml"
export TALOSCONFIG="${ROOT_DIR}/talosconfig"

KUBERNETES_VERSION="$(yq ".global.kubernetesVersion" "${ROOT_DIR}/kubernetes/clusters/$(cat .current-cluster)/values.yaml")"
export KUBERNETES_VERSION
TALOS_VERSION="$(yq ".global.talosVersion" "${ROOT_DIR}/kubernetes/clusters/$(cat .current-cluster)/values.yaml")"
export TALOS_VERSION
