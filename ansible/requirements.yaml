---
# yaml-language-server: $schema=https://schemas.rzegocki.dev/other/ansible-requirements.json
# Bundled with pkgs.ansible, so devenv needs no extra step. On a plain ansible-core
# control node: ansible-galaxy collection install -r requirements.yaml
collections:
  - name: community.crypto
  - name: community.docker
