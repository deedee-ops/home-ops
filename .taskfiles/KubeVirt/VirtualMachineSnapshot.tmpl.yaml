---
# yaml-language-server: $schema=https://deedee-ops.github.io/schemas/snapshot.kubevirt.io/virtualmachinesnapshot_v1alpha1.json
apiVersion: snapshot.kubevirt.io/v1alpha1
kind: VirtualMachineSnapshot
metadata:
  name: "${vm}-${tstamp}"
spec:
  source:
    apiGroup: kubevirt.io
    kind: VirtualMachine
    name: "${vm}"
