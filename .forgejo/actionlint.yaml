---
self-hosted-runner:
  labels:
    - docker
paths:
  .forgejo/workflows/**/*.yaml:
    ignore:
      - >-
        invalid format because owner and repo and ref should not be empty. available formats are "{owner}/{repo}@{ref}"
        or "{owner}/{repo}/{path}@{ref}"
      - 'undefined variable "forgejo". available variables are'
