---
# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3.json
tls:
  stores:
    default:
      defaultGeneratedCert:
        resolver: vault-ecdsa
  options:
    rsa-kex:
      maxVersion: VersionTLS12
      minVersion: VersionTLS12
