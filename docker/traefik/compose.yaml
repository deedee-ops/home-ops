---
# yaml-language-server: $schema=https://schemas.ajgon.casa/other/docker-compose.json
services:
  traefik:
    environment:
      CF_DNS_API_TOKEN: "${CF_DNS_API_TOKEN}"
      GODEBUG: "tlsrsakex=1"
      TZ: Europe/Warsaw
    image: public.ecr.aws/docker/library/traefik:v3.7.5@sha256:e4d98158c01ad752fc1071d4e9573788747230d902cdde00a772516e692d07c9
    container_name: traefik
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "80:80"
      - "443:443"
    configs:
      - source: traefik
        target: /etc/traefik/traefik.yml
      - source: extra-dashboard
        target: /config/extra/dashboard.yaml
      - source: extra-tls
        target: /config/extra/tls.yaml
      - source: extra-truenas
        target: /config/extra/truenas.yaml
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${DATA_DOCKER_DIR}/traefik/certs:/certs
      - ${DATA_DOCKER_DIR}/vault/file/cert.pem:/vault-internal.pem
    networks:
      - default
      - internal
    # security
    cap_drop:
      - all
    security_opt:
      - no-new-privileges
    read_only: true
  traefik-to-unifi:
    environment:
      IGNORE_SSL_WARNINGS: "true"
      TRAEFIK_API_URL: "http://traefik:8080/api/"
      TRAEFIK_IP: "${TRAEFIK_IP}"
      UNIFI_API_KEY: "${UNIFI_API_KEY}"
      UNIFI_URL: "${UNIFI_URL}"
      UNIFI_USERNAME: "${UNIFI_USERNAME}"
    image: ghcr.io/thomaslomas/traefik-to-unifi:v1.3.0@sha256:0aeeeb8171da8268edec8d87d599f129aded778ba98eb5eac1eccc5f10bcbcec
    container_name: traefik-to-unifi
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # security
    cap_drop:
      - all
    security_opt:
      - no-new-privileges
    read_only: true

configs:
  traefik:
    file: ./config/traefik.yaml
  extra-dashboard:
    file: ./config/extra/dashboard.yaml
  extra-tls:
    file: ./config/extra/tls.yaml
  extra-truenas:
    file: ./config/extra/truenas.yaml

networks:
  internal:
    name: internal
    external: true
